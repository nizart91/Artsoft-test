{% extends 'base.html.twig' %}

{% block title %}{{ book.name }} ({{ book.year }})!{% endblock %}

{% block body %}
    <div class="container">
        <h2>Book {{ book.name }}</h2>
        <h4>year: {{ book.year }}</h4>
        <br>
        <a href="{{ path('update_book', {'book': book.id}) }}">Edit</a>
        <br>
        <a href="{{ path('delete_book', {'book': book.id}) }}">Remove</a>
    </div>
{% endblock %}
